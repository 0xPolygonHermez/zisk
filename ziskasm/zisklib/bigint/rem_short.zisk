; ============================================================================
; bigint/rem_short.zisk - remainder of a large number modulo a short number. Mirrors rem_short.rs.
; GENERATED by scratchpad/gen_bigint.py. Radix 2^256 (digit = U256 = 4 u64);
; arbitrary-precision arithmetic for modexp (EIP-198). Prefix bi_.
; ============================================================================

; void zisklib_rem_short_init(const u64* a, u64 lena_u256, const u64* b, u64* result)
;   result = a mod b (b is one U256). hint (q,r) then verify a == q*b + r, r < b.
zisklib_rem_short_init:
	push r1
	copyb(0, BI_RS_A) -> r5
	copyb(r5, r10) -> 8[a + 0]
	copyb(0, BI_RS_LEN) -> r5
	copyb(r5, r11) -> 8[a + 0]
	copyb(0, BI_RS_B) -> r5
	copyb(r5, r12) -> 8[a + 0]
	copyb(0, BI_RS_RES) -> r5
	copyb(r5, r13) -> 8[a + 0]
	copyb(0, 1) -> r5
	copyb(0, BI_RS_LEN) -> r6
	copyb(r6, 8[a + 0]) -> r6
	eq(r6, r5), j(bi_rs_len1)
	jump(bi_rs_general)
bi_rs_len1:
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_RS_B) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_ltn
	eq(r10, 0), j(bi_rs_ge)
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_RS_RES) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_memcpy
	pop r1
	ret
bi_rs_ge:
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_RS_B) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_eqn
	eq(r10, 0), j(bi_rs_general)
	copyb(0, BI_ZERO) -> r10
	copyb(0, BI_RS_RES) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_memcpy
	pop r1
	ret
bi_rs_general:
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_RS_LEN) -> r5
	copyb(r5, 8[a + 0]) -> r11
	sll(r11, 2) -> r11
	copyb(0, BI_RS_B) -> r12
	copyb(r12, 8[a + 0]) -> r12
	copyb(0, 4) -> r13
	copyb(0, BI_QUO) -> r14
	copyb(0, BI_REM) -> r15
	call zisklib_bigint_div
	srl(r10, 2) -> r10
	copyb(0, BI_RS_QLEN) -> r5
	copyb(r5, r10) -> 8[a + 0]
	copyb(0, BI_QUO) -> r10
	copyb(0, BI_RS_QLEN) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, BI_RS_B) -> r12
	copyb(r12, 8[a + 0]) -> r12
	copyb(0, BI_QB) -> r13
	call zisklib_mul_short
	copyb(0, BI_RS_QBLEN) -> r5
	copyb(r5, r10) -> 8[a + 0]
	copyb(0, BI_REM) -> r10
	copyb(0, BI_ZERO) -> r11
	copyb(0, 4) -> r12
	call bn254_eqn
	ltu(0, r10), j(bi_rs_remzero)
	copyb(0, BI_REM) -> r10
	copyb(0, BI_RS_B) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_ltn
	eq(r10, 0), j(bi_rs_bad)
	copyb(0, BI_QB) -> r10
	copyb(0, BI_RS_QBLEN) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, BI_REM) -> r12
	copyb(0, BI_QBR) -> r13
	call zisklib_add_short
	copyb(0, BI_RS_LEN) -> r5
	copyb(r5, 8[a + 0]) -> r6
	eq(r10, r6), j(bi_rs_cmp_qbr)
	jump(bi_rs_bad)
bi_rs_cmp_qbr:
	sll(r10, 2) -> r12
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_QBR) -> r11
	call bn254_eqn
	eq(r10, 0), j(bi_rs_bad)
	jump(bi_rs_setres)
bi_rs_remzero:
	copyb(0, BI_RS_QBLEN) -> r5
	copyb(r5, 8[a + 0]) -> r10
	copyb(0, BI_RS_LEN) -> r5
	copyb(r5, 8[a + 0]) -> r6
	eq(r10, r6), j(bi_rs_cmp_qb)
	jump(bi_rs_bad)
bi_rs_cmp_qb:
	copyb(0, BI_RS_LEN) -> r5
	copyb(r5, 8[a + 0]) -> r12
	sll(r12, 2) -> r12
	copyb(0, BI_RS_A) -> r10
	copyb(r10, 8[a + 0]) -> r10
	copyb(0, BI_QB) -> r11
	call bn254_eqn
	eq(r10, 0), j(bi_rs_bad)
bi_rs_setres:
	copyb(0, BI_REM) -> r10
	copyb(0, BI_RS_RES) -> r11
	copyb(r11, 8[a + 0]) -> r11
	copyb(0, 4) -> r12
	call bn254_memcpy
	pop r1
	ret
bi_rs_bad:
	copyb(0, 0) -> r10, end

