[package]
name = "multiple-programs-host"
version = "0.1.0"
edition = "2021"

[dependencies]
zisk-sdk = { workspace = true }
test-artifacts = { workspace = true, features = ["fib_mod"] }

[features]
default = []
gpu = []
