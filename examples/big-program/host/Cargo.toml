[package]
name = "big-program-host"
version = "0.1.0"
edition = "2021"

[dependencies]
zisk-sdk = { workspace = true }
test-artifacts = { workspace = true, features = ["big_input"] }

[build-dependencies]
zisk-sdk = { workspace = true }

[features]
default = []
gpu = []
