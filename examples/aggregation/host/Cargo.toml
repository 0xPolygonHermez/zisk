[package]
name = "aggregation-host"
version = "0.1.0"
edition = "2021"

[dependencies]
zisk-sdk = { workspace = true }
zisk-test-artifacts = { workspace = true, features = ["fib_mod", "agg_verify"] }
serde = { version = "1.0", default-features = false, features = ["derive"] }

[features]
default = []
gpu = []
