// @generated by `cargo build -p zisk-definitions-sync` — do not edit.

// log2 of the maximum step count. Reads better in decimal.
const int MAIN_STEP_BITS = 36;
// Maximum number of execution steps (a hard PIL constraint). Exceeds 32 bits, so widen the inherited fit check.
const int MAX_STEPS      = 0x1000000000;  // 1u64 << MAIN_STEP_BITS
