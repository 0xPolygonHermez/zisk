require "std_range_check.pil";

airtemplate MultiRangeCheck(const int N = 2**3) {

    col witness a;
    col witness sel;
    col witness range_sel;

    multi_range_check(sel, range_sel, a, 0, 2**7-1, 0, 2**8-1);
};

airgroup MultiRangeCheck {
    MultiRangeCheck();
}