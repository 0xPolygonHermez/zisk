global:
  scrape_interval:     5s   # 10s jobs → fine resolution. Workers' /metrics
  evaluation_interval: 5s   # endpoint will join later as a second scrape job.

scrape_configs:
  - job_name: zisk-coordinator
    static_configs:
      - targets: ["coordinator:9091"]
    metrics_path: /metrics
