# coordinator.toml — ZisK Coordinator Docker configuration
#
# Mounted into the coordinator container at /etc/zisk/coordinator.toml.
# Override individual values via environment variables:
#   ZISK_COORDINATOR__SERVER__PORT=8000
#   ZISK_COORDINATOR__LOGGING__LEVEL=debug

[service]
name = "ZisK Coordinator"
environment = "production"

[server]
host = "0.0.0.0"
port = 7001
shutdown_timeout_seconds = 30

[metrics]
enabled = true
host = "0.0.0.0"
port = 9091

[logging]
level = "info"
format = "json"

[backend]
mode = "coordinator"

[coordinator]
# Port on which the embedded coordinator listens for worker connections.
# Workers must connect to this port on the coordinator host/container.
worker_port = 50052
# config_file = "/etc/zisk/coordinator.toml"  # optional: tune coordinator internals
