# Example inventory for ZisK distributed deployment.
# Copy to inventory.ini and adjust hosts/SSH settings for your environment.

[coordinator]
coord01.example.com

[workers]
worker01.example.com
worker02.example.com
worker03.example.com

[workers:vars]
ansible_user = deploy
ansible_become = yes

[coordinator:vars]
ansible_user = deploy
ansible_become = yes
