[package]
name = "zisk-coordinator-contract"
description = "Extracts the metric + route contract from coordinator-server source so dashboard validators stay in sync without manual allowlists"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "extract-coordinator-contract"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
