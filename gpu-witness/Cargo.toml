[package]
name = "zisk-gpu-witness"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
keywords = { workspace = true }
repository = { workspace = true }
categories = { workspace = true }
publish = false

# The Rust half of the GPU witness-kernel contract: the FFI declarations a
# kernel crate would otherwise hand-write, and the comparison harness that
# proves a kernel matches its CPU path. The build half is `zisk-cuda-build`.
[dependencies]
paste = { workspace = true }
