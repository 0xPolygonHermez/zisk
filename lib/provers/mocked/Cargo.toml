[package]
name = "prover-mocked"
version = "0.1.0"
authors = ["Polygon Labs"]
edition = "2021"
readme = "README.md"

[lib]
path = "src/lib.rs"

[dependencies]
proofman = { path = "../../../proofman-old" }
util = { path = "../../../util" }
log = { version = "0.4", default-features = false }
