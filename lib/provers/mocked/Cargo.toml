[package]
name = "prover-mocked"
version = "0.0.1"
authors = ["Polygon Labs"]
edition = "2021"
readme = "README.md"

[lib]
path = "src/lib.rs"

[dependencies]
proofman = { path = "../../../proofman" }
util = { path = "../../../util" }
log = { version = "0.4", default-features = false }
