require "std_sum.pil"

const string name_lookup = "lookup";

function lookup_assumes(const int opid, const expr sel = 1, const expr cols[]) {
    sum_assumes(name_lookup, opid, sel, cols);
}

function lookup_proves(const int opid, const expr mul = 1, const expr cols[]) {
    sum_proves(name_lookup, opid, mul, cols);
}
