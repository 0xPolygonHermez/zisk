[package]
name = "zisk-cuda-build"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
keywords = { workspace = true }
repository = { workspace = true }
categories = { workspace = true }
publish = false

# Build-script support for crates that ship CUDA kernels. Used from `build.rs`
# as a `[build-dependencies]` entry; see `compile`.
[dependencies]
