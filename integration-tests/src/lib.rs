//! Integration tests for ZisK that run guest ELFs end-to-end.
//!
//! This crate intentionally has no public API — see the `tests/` directory for
//! the actual test suites (e.g. `zisklib`, which executes the zisklib precompile
//! guest programs through the emulator).
