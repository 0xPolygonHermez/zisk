// ZisK generated execution constants.
require "execution.gen.pil"

const int MEM_STEP_BITS = MAIN_STEP_BITS + 2;
const int REG_STEP_BITS = MAIN_STEP_BITS + 2;
const int ADDR_BITS = 32;
const int ADDR_W_BITS = ADDR_BITS - 3;
const int EXTRA_PARAMS_ADDR = 0xA0400F00;