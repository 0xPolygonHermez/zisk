[package]
name = "profile-viewer"
version = "0.1.0"
edition = "2021"

# Empty workspace to keep this as an independent project
[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
flate2 = "1.0"
clap = { version = "4.0", features = ["derive"] }
